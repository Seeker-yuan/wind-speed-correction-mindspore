# 风场全局同步时空图模型技术说明

## 1. 目标与约束

本项目针对风电场 108 台风机小时级风速序列，完成缺失补全与测量误差校正。模型设计严格围绕导师提出的两条核心要求展开：

1. 前段“找联系”：不做局部筛选，构建覆盖全场的统一关系拓扑。
2. 后段“同步走”：不做单机独立推理，进行全场联合演化预测。

因此，系统采用“全局多视图拓扑构建 + 全局掩码时空图网络 + 逐时同步补全”的闭环方案。

## 2. 方法总览

算法流程分为两阶段：

1. 全局关系图构建阶段：离线或半离线地构建 108x108 全局邻接矩阵。
2. 全局同步演化阶段：训练单一全局模型，并按时间顺序同步补全缺失值。

整体输入输出定义如下：

- 输入：$X\in\mathbb{R}^{N\times108\times L}$，$L$ 为历史窗口长度。
- 输出：$\hat{Y}\in\mathbb{R}^{N\times108}$，对应下一时刻全场预测。

## 3. 导师要求一：全局关系图构建（前段找联系）

### 3.1 三视图关系矩阵

对任意风机对 $(i,j)$ 构建三类关系：

1. 地理关系 $A_{geo}$：基于经纬度距离的高斯核相似度。
2. 波形关系 $A_{dtw}$：基于历史序列 DTW 距离的形态相似度。
3. 线性关系 $A_{corr}$：基于 Pearson 相关系数映射到 $[0,1]$。

### 3.2 融合与归一化

融合矩阵定义为：

$$
A_{global}=w_{geo}A_{geo}+w_{dtw}A_{dtw}+w_{corr}A_{corr}
$$

默认权重：$w_{geo}=0.4,\;w_{dtw}=0.3,\;w_{corr}=0.3$。

加入自环并行归一化：

$$
\\tilde{A}=A_{global}+I,\\qquad \\hat{A}=D^{-1}\\tilde{A}
$$

其中 $D$ 为度矩阵。最终使用 $\hat{A}$ 进行图消息传递。

### 3.3 工程化缓存机制

考虑到 DTW 计算复杂度较高，系统已实现邻接矩阵缓存：

1. 首次构建后保存 `cache/A_global.npy`。
2. 同步保存配置元信息 `cache/A_global.meta.json`（节点列表、时间范围、权重、下采样率等）。
3. 后续若元信息一致，直接加载缓存并跳过 DTW 重算。

该机制显著缩短重复运行时延，满足演示与迭代开发的效率要求。

## 4. 导师要求二：全场同步演化（后段同步走）

### 4.1 全局样本构造

对每个时刻 $t$，构造：

$$
X_t=[x_{t-L},x_{t-L+1},\ldots,x_{t-1}],\qquad y_t=x_t
$$

其中 $x_t\in\mathbb{R}^{108}$。即模型一次输入全场历史窗，一次输出全场下一时刻。

### 4.2 因果性约束

为杜绝时间泄露，特征填补仅允许前向信息：

1. 对特征窗执行 `ffill()`。
2. 对序列起点仍缺失的位置填 `0.0`。
3. 禁止 `bfill()` 与全局均值回填。

因此，$t$ 时刻输入严格不包含 $t$ 之后信息，满足时序因果律。

### 4.3 掩码监督与 NaN 一致性

标签 $y$ 保留 NaN 到归一化阶段，由掩码与 NaN-aware 统计共同处理：

1. 统计量使用 $\text{nanmean}/\text{nanstd}$ 计算，避免缺失污染均值方差。
2. 归一化后仅将 NaN 转为 0 作为张量占位。
3. 损失函数只在有效观测节点计算：

$$
\mathcal{L}=\frac{\sum_i m_i(\hat{y}_i-y_i)^2}{\sum_i m_i+\epsilon}
$$

其中 $m_i\in\{0,1\}$。该机制保证“缺失不监督、有效才回传梯度”。

## 5. 全局时空图网络结构

网络主干包含：

1. 输入投影层：将每个节点长度为 $L$ 的时序特征映射到隐藏空间。
2. 4 层图卷积：在全局拓扑 $\hat{A}$ 上进行消息传递。
3. 残差连接：每两层进行残差更新，提升深层稳定性。
4. 节点注意力：对节点状态进行自适应加权。
5. 输出头：节点级 MLP 输出全场预测。

图卷积核心形式：

$$
H^{(l+1)}=\mathrm{ReLU}(\hat{A}H^{(l)}W^{(l)})
$$

## 6. 同步补全推理机制

按时间轴逐小时执行：

1. 抽取当前时刻之前 $L$ 小时全场上下文。
2. 全局模型输出当前时刻 108 维预测向量。
3. 仅回填当前时刻缺失节点，已观测节点保持原值。
4. 时间前移并重复，完成整段序列同步补全。

这种“全场统一前推”机制可在多机组并发缺失场景下保持鲁棒性。

## 7. 运行模式与硬件配置

MindSpore 运行模式支持环境变量配置：

1. `MS_MODE=GRAPH` 或 `MS_MODE=PYNATIVE`。
2. `MS_DEVICE_TARGET=CPU` 或 `MS_DEVICE_TARGET=ASCEND`。

默认配置为 `GRAPH + CPU`，以兼顾通用环境下的图执行效率。部署到昇腾环境时可切换 `MS_DEVICE_TARGET=ASCEND` 以获得更高吞吐。

## 8. 默认参数与输出

默认参数：

1. 节点数：108
2. 时间窗长度：6
3. 训练轮数：10
4. 隐藏维度：64
5. 邻接融合权重：0.4 / 0.3 / 0.3
6. DTW 下采样步长：3

输出文件位于 [cleaned_data](cleaned_data)，每台风机一个 xlsx，字段含义：

1. OBS_raw：原始观测
2. OBS：补全后观测
3. filled：补全标记（1 为补全，0 为原始）

## 9. 关键实现文件

1. 主流程：[预测_mindspore.py](预测_mindspore.py)
2. 模型定义：[mindspore_gnn_model.py](mindspore_gnn_model.py)

## 10. 运行命令

```bash
"D:\Anaconda_envs\envs\mindspore\python.exe" "C:\Users\31876\Desktop\风能ui设计\预测_mindspore.py"
```

## 11. 结论

当前系统已完整落实导师提出的两项核心要求：

1. 在前段实现全局关系挖掘，而非局部临时筛选。
2. 在后段实现全场同步演化，而非单机独立补全。

同时，通过因果性约束、NaN-aware 归一化与掩码损失，保证了时序建模的学术严谨性；通过邻接矩阵缓存和可配置执行模式，保证了工程可落地性。
