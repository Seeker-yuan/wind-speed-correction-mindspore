# 风场全局同步时空图模型技术说明

## 1. 任务定义

本项目面向风电场 108 台风机的小时级风速数据缺失补全与误差校正。算法将全场机组视为统一时空系统，基于全局图拓扑进行同步推理，在每个时间步联合预测全场节点状态。

## 2. 总体流程

算法由两个阶段组成：

1. 全局关系图构建：利用地理关系与历史观测关系生成全场邻接矩阵 $A_{global}$。
2. 全局同步演化：以滑动时间窗构造训练样本，训练单一全局图网络，并按时间顺序对缺失数据进行同步补全。

## 3. 全局多视图邻接矩阵

对 108 台风机构造三类相似度矩阵：

1. 地理相似度矩阵 $A_{geo}$：由经纬度距离经高斯核映射得到。
2. 形态相似度矩阵 $A_{dtw}$：基于历史序列 DTW 距离映射得到。
3. 相关性矩阵 $A_{corr}$：由 Pearson 相关系数映射到 $[0,1]$ 区间。

融合方式：

$$
A_{global}=w_{geo}A_{geo}+w_{dtw}A_{dtw}+w_{corr}A_{corr}
$$

其中默认权重为 $w_{geo}=0.4, w_{dtw}=0.3, w_{corr}=0.3$。

随后进行自环增强与行归一化：

$$
\\tilde{A}=A_{global}+I,\\qquad \\hat{A}=D^{-1}\\tilde{A}
$$

## 4. 全局时空图网络

### 4.1 输入输出定义

- 输入张量：$(N,108,L)$，其中 $N$ 为样本数，$L$ 为时间窗长度（默认 6）。
- 输出张量：$(N,108)$，对应下一时刻全场 108 台风机预测值。

### 4.2 网络结构

1. 输入投影层：将每个节点的时间窗特征映射到隐藏维度。
2. 4 层图卷积模块：每层执行消息传递与非线性变换。
3. 残差连接：每两层进行一次残差更新。
4. 节点注意力：对节点表示进行自适应加权。
5. 输出 MLP：映射为节点级风速预测。

图卷积核心计算：

$$
H^{(l+1)}=\mathrm{ReLU}(\hat{A}H^{(l)}W^{(l)})
$$

## 5. 训练策略

### 5.1 训练样本构建

对每个时间点 $t$，取前 $L$ 小时全场数据作为输入，目标为 $t$ 时刻全场观测：

$$
X_t=[x_{t-L},\dots,x_{t-1}],\qquad y_t=x_t
$$

其中 $x_t\in\mathbb{R}^{108}$。

### 5.2 缺失掩码损失

当标签中存在缺失值时，采用掩码损失，仅对有效观测节点计算误差：

$$
\mathcal{L}=\frac{\sum_i m_i(\hat{y}_i-y_i)^2}{\sum_i m_i+\epsilon}
$$

其中 $m_i\in\{0,1\}$ 为节点有效性掩码。

## 6. 同步补全机制

按时间顺序执行逐时推理：

1. 取当前时刻前 $L$ 小时全场上下文。
2. 由全局模型输出当前时刻全场 108 维预测。
3. 仅替换当前时刻缺失节点，非缺失节点保持原值。
4. 时间步前移，重复执行直至序列结束。

该机制实现全场节点在统一拓扑上的同步演化预测。

## 7. 实现文件与参数

- 主流程脚本：[预测_mindspore.py](预测_mindspore.py)
- 模型定义文件：[mindspore_gnn_model.py](mindspore_gnn_model.py)

默认参数：

- 节点数：108
- 时间窗长度：6
- 训练轮数：10
- 隐藏维度：64
- 优化器：Adam，学习率 0.001

## 8. 运行方式

```bash
"D:\Anaconda_envs\envs\mindspore\python.exe" "C:\Users\31876\Desktop\风能ui设计\预测_mindspore.py"
```

输出目录为 [cleaned_data](cleaned_data)，每台风机对应一个 xlsx 文件，包含以下字段：

- OBS_raw：原始观测
- OBS：补全后序列
- filled：补全标记（1 表示补全，0 表示原始）
